context API - share/store data as state without needing to pass as props through every level of the component tree - alternative to Redux
-context provider: considered "global" for a tree of React components.
